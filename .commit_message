feat: full ingest pipeline — reference data + PDF OCR + chunking + Qdrant

- Added TopicPoint model and Qdrant topics collection with auto-sync
- Fixed StubGetContentHandler: now downloads PDF + runs Yandex Vision OCR
- Fixed PDF download URL (/file/pdf instead of /api/Document/Pdf)
- Created scripts/full_pipeline.py — cleans DB/Qdrant, loads references,
  ingests 6 documents from pravo.gov.ru via stub adapter with real OCR
- Admin API: GET /api/v1/admin/reference-counts, /admin/qdrant/collections,
  /admin/documents/{publish_id}/status
- Removed obsolete intermediate scripts
- Added output/ to .gitignore
